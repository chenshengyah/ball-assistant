{
  "name": "ball-assistant",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.8.0",
  "scripts": {
    "dev": "pnpm dev:backend",
    "dev:all": "turbo run dev --parallel",
    "dev:backend": "pnpm db:up && pnpm db:migrate && pnpm --filter @ball-assistant/api dev",
    "db:up": "docker compose -f infra/docker-compose.yml up -d",
    "db:migrate": "pnpm --dir apps/api exec prisma migrate deploy",
    "db:down": "docker compose -f infra/docker-compose.yml down",
    "miniapp:env:local": "MINIAPP_RUNTIME_ENV=local pnpm --filter @ball-assistant/miniapp env:generate",
    "miniapp:env:production": "MINIAPP_RUNTIME_ENV=production pnpm --filter @ball-assistant/miniapp env:generate",
    "miniapp:upload:dev": "pnpm --filter @ball-assistant/miniapp upload:dev",
    "miniapp:upload:experience": "pnpm --filter @ball-assistant/miniapp upload:experience",
    "api:image:build": "pnpm --filter @ball-assistant/api image:build",
    "api:image:push": "pnpm --filter @ball-assistant/api image:push",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "eslint": "^9.25.0",
    "globals": "^15.15.0",
    "prettier": "^3.5.3",
    "turbo": "^2.5.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.30.1"
  }
}
